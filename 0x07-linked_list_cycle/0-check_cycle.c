#include "lists.h"
#include <stdio.h>

/**
 * check_cycle - Computes the sum of two sandpiles.
 *
 * @list: 3x3 stable sandpile.
 * Return: 0
 */
int check_cycle(listint_t *list) {
	printf("%i", list->n);
	return(0);
}
